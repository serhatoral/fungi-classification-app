import 'package:flutter/material.dart';

class PoisonAlert extends StatelessWidget {
  const PoisonAlert({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/icons8-poison-50.png', height: 40, width: 50,);
  }
}