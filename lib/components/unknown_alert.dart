import 'package:flutter/material.dart';

class UnknownAlert extends StatelessWidget {
  const UnknownAlert({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/icons8-unknown-48.png', height: 45, width: 50, );
  }
}