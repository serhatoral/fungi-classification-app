import 'package:flutter/material.dart';

class EdibleAlert extends StatelessWidget {
  const EdibleAlert({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/icons8-edible-48.png', height: 45, width: 50, );
  }
}